name: Security

on:
  push:
    branches: ["**"]
  pull_request:
  schedule:
    - cron: '0 8 * * 1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  scan:
    permissions:
      contents: read
      packages: read
    uses: garymike/security-workflows/.github/workflows/security-scan.yml@a812eda886a4198d6aac095aba9e321893e677ed # v0.4.1
    secrets: inherit

  gha-security:
    permissions:
      contents: read
      packages: read
    uses: garymike/security-workflows/.github/workflows/gha-security.yml@a812eda886a4198d6aac095aba9e321893e677ed # v0.4.1
    secrets: inherit

  sast:
    permissions:
      contents: read
      packages: read
      security-events: write
    uses: garymike/security-workflows/.github/workflows/sast.yml@a812eda886a4198d6aac095aba9e321893e677ed # v0.4.1
    secrets: inherit

  iac-security:
    permissions:
      contents: read
      packages: read
      security-events: write
    uses: garymike/security-workflows/.github/workflows/iac-security.yml@a812eda886a4198d6aac095aba9e321893e677ed # v0.4.1
    secrets: inherit

  codeql:
    # deep engine — run on PRs, main pushes, and the schedule (skip feature-branch pushes)
    if: github.event_name != 'push' || github.ref == 'refs/heads/main'
    permissions:
      contents: read
      security-events: write
      actions: read
    uses: garymike/security-workflows/.github/workflows/codeql.yml@a812eda886a4198d6aac095aba9e321893e677ed # v0.4.1
    with:
      languages: javascript-typescript,python

  audit:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    permissions:
      contents: read
    uses: garymike/security-workflows/.github/workflows/security-audit.yml@a812eda886a4198d6aac095aba9e321893e677ed # v0.4.1
    secrets: inherit
