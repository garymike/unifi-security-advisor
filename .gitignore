# Node
node_modules/

# Build outputs
dist/
build/
.svelte-kit/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/

# Rust / Tauri
src-tauri/target/

# Environment / secrets
.env
.env.local
*.env

# Audit output — contains real network data (device names, site IDs)
audit_output/

# Raw UniFi backup files — real, unsanitized network data. Never commit
# these directly; anonymize into a samples/fixture-*.json first.
*.unf
*.unifi

# Claude Code superpowers scratch state
.superpowers/

# OS
.DS_Store
Thumbs.db

# Generated by npm run check-advisory-drift
advisory-drift-report.md

# Generated by npm run check-api-drift
api-drift-report.md

# Tauri updater / code-signing keys — never commit (private key signs updates the app trusts)
*.key
*.key.pub
.tauri/

# Local-only PII regression list for fixtureCgfBackupSafety (never commit real leaked values)
known-leaks.local.json

# Local Claude Code config (dev-only, not part of the project)
.claude/
